import express from "express";

const router = express.Router();

/*
router.get("/", cafeSearch);
router.get("/filter", cafeFilter);
router.get("/map", cafeMap);
router.post("/map", cafeMapPost);
*/
export default router;
